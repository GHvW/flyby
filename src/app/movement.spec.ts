
import { expect, test, describe } from 'vitest'
// import { movementStateGraph, MoveKeyCombo, MovementEvent } from "./controlStreams";
// import { stateTransitions } from "./stateGraph";

describe("movement", () => {

    // const machine = stateTransitions(movementStateGraph);

    // test("does stuff", () => {
    //     const result = machine(MoveKeyCombo.Up, MovementEvent.KeyupUp);

    //     expect(result).toBe(MoveKeyCombo.None);
    // });
    test("it", () => {
        expect(1 + 1).toBe(2);
    })
});